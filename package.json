{
  "name": "dopamine-platform",
  "private": true,
  "version": "0.1.0",
  "description": "dopamine platform monorepo",
  "workspaces": [
    "web"
  ],
  "scripts": {
    "dev": "npm run dev -w web",
    "build": "npm run build -w web",
    "lint": "npm run lint -w web",
    "db:push": "drizzle-kit push --config=web/drizzle.config.ts"
  },
  "devDependencies": {
    "drizzle-kit": "^0.31.10",
    "drizzle-orm": "^0.45.2"
  }
}
